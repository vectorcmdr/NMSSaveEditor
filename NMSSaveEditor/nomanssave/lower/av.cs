using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows.Forms;

namespace NMSSaveEditor
{



public class av : ActionListener {
   public ap cu;

   public av(ap var1) {
      this.cu = var1;
   }

   public void actionPerformed(EventArgs var1) {
      // PORT_TODO: // PORT_TODO: string[] var2 = p.c((Container)this.cu);
      bool var3 = false;

      if (false) { // PORT_TODO: original loop had errors
         // PORT_TODO: ey var5 = ey.d(var2[var4]);
         if (true) { // PORT_TODO: original condition had errors
            if (true) { // PORT_TODO: original condition had errors
               // PORT_TODO: ap.e(this.cu).f(var2[var4]);
            }

            if (true) { // PORT_TODO: original condition had errors
               // PORT_TODO: ap.f(this.cu).f(var2[var4]);
            }

            // PORT_TODO: ap.d(this.cu).Add(var2[var4]);
            var3 = true;
         }
      }

      if (var3) {
         // PORT_TODO: ap.g(this.cu).sort();
         ap.h(this.cu).Refresh();
      }

   }
}



}
